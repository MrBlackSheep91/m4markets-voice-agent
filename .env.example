# M4Markets Voice Agent - Environment Configuration
# Copy this file to .env and fill in your values

# ============================================================================
# CORE SERVICES (Required)
# ============================================================================

# LiveKit Configuration
LIVEKIT_URL=wss://your-instance.livekit.cloud
LIVEKIT_API_KEY=your-api-key
LIVEKIT_API_SECRET=your-api-secret

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key

# Database Configuration (Neon PostgreSQL)
DB_URL=postgresql://user:password@host/database

# ============================================================================
# VOICE OPTIMIZATION (Optional - for latency tuning)
# ============================================================================

# Voice Selection
# Options: alloy, echo, fable, onyx, nova, shimmer
# Recommended for speed: nova (default), alloy, echo
# Recommended for realism: fable, shimmer
AGENT_VOICE=nova

# Voice Speed (0.25 to 4.0)
# 1.0 = normal speed
# 1.15 = slightly faster (recommended for lower latency)
# 1.25 = faster (if latency is critical)
# 0.9 = slower (for clarity)
VOICE_SPEED=1.15

# ============================================================================
# INTEGRATIONS (Optional)
# ============================================================================

# Evolution API (WhatsApp)
EVOLUTION_API_URL=https://your-evolution-api.railway.app
EVOLUTION_API_KEY=your-evolution-api-key
EVOLUTION_INSTANCE_NAME=your-instance-name

# Frontend URL
FRONTEND_URL=https://your-frontend.netlify.app

# ChromaDB (Second Brain MCP)
CHROMA_URL=http://your-chroma-instance:8000

# Deepgram (Alternative STT - not currently used)
DEEPGRAM_API_KEY=your-deepgram-api-key

# ============================================================================
# LOGGING & MONITORING (Optional)
# ============================================================================

# Log Level
# Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Langfuse Observability (Optional - for advanced cost tracking and tracing)
# Get your keys from https://cloud.langfuse.com
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key
LANGFUSE_HOST=https://cloud.langfuse.com

# ============================================================================
# VOICE QUALITY vs LATENCY PRESETS
# ============================================================================
# Uncomment one of the presets below:

# === PRESET 1: ULTRA LOW LATENCY (Fastest, less realistic) ===
# AGENT_VOICE=echo
# VOICE_SPEED=1.25

# === PRESET 2: BALANCED (Recommended - DEFAULT) ===
# AGENT_VOICE=nova
# VOICE_SPEED=1.15

# === PRESET 3: HIGH QUALITY (More realistic, higher latency) ===
# AGENT_VOICE=fable
# VOICE_SPEED=1.0

# === PRESET 4: NATURAL (Most human-like) ===
# AGENT_VOICE=shimmer
# VOICE_SPEED=0.95

# ============================================================================
# COST OPTIMIZATION NOTES
# ============================================================================
#
# PRICING (as of 2025):
# - Whisper STT: $0.006/minute
# - GPT-4o-mini: $0.15/1M input tokens, $0.60/1M output tokens
# - TTS: $15/1M characters
#
# COST REDUCTION TIPS:
# 1. Use faster voice (echo/nova) to reduce call duration
# 2. Optimize system instructions to reduce LLM tokens
# 3. Cache frequently asked questions
# 4. Set timeout for inactive calls
#
# ESTIMATED COSTS:
# - 1 minute call: ~$0.02-0.05
# - Average qualified lead call (5-8 min): ~$0.15-0.30
# - Hot lead call (10-15 min): ~$0.40-0.70
#
# ============================================================================
